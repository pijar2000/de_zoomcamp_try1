# Before You Start

DE zoomcamp make lot of bash command, you shoul install wsl with your powershell with this command

```powershell
wsl --install
```
it should installing ubuntu in your wsl system

after that you can start ubuntu terminal with this command

```powershell
wsl.exe -d Ubuntu
```
Careful, ubuntu terminal use bash, so it a bit different to go to another folder
